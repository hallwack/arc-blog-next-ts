import { NextPage } from "next";
import React, { FunctionComponent } from "react";

interface Props {}

const CategoryIndex: FunctionComponent<NextPage> = (props: Props) => {
  return <div></div>;
};

export default CategoryIndex;
